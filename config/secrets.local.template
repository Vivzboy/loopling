# Copy to ~/.claude/secrets.local (gitignored) — never commit real secrets.
# The bot reads these when it needs a credential. Add only what your bot uses.

# Telegram (this bot)
LOOPLING_BOT_TOKEN=
LOOPLING_OWNER_CHAT_ID=

# Any API keys your bot's purpose needs, e.g.:
# OPENAI_API_KEY=
# SOME_SERVICE_API_KEY=
